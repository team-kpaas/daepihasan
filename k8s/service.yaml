apiVersion: v1
kind: Service
metadata:
  name: my-prj-svc
  namespace: default
  labels:
    app: my-prj
spec:
  selector:
    app: my-prj
    tier: backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: LoadBalancer
